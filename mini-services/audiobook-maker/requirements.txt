ruff
boto3
flask-compress
google-auth>=2.23  # FCM HTTP v1 push notifications (app mobile)
requests  # client HTTP per push FCM (push_service)
qrcode[pil]>=7.4  # QR di trasferimento job sull'app mobile
pymupdf  # PDF text extraction + chapter detection (pdf_to_tts.py)
